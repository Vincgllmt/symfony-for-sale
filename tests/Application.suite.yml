actor: ApplicationTester
suite_namespace: App\Tests\Application
modules:
    # enable helpers as array
    enabled:
        - Symfony:
              app_path: 'src'
              environment: 'test'
        - Doctrine2:
              depends: Symfony
              cleanup: true
        - Asserts: